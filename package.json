{
  "scripts": {
    "clean": "rimraf dist/",
    "build": "npm run clean && tsc"
  },
  "devDependencies": {
    "rimraf": "^5.0.1",
    "@types/bcrypt": "^5.0.0",
    "@types/express": "^4.17.21"
  }
} 