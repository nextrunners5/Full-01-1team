# 모든 텍스트 파일의 줄 바꿈 문자를 LF로 통일
* text=auto eol=lf

# 특정 파일 형식에 대한 설정
*.{cmd,[cC][mM][dD]} text eol=crlf
*.{bat,[bB][aA][tT]} text eol=crlf
*.sh text eol=lf

# 바이너리 파일은 변환하지 않음
*.png binary
*.jpg binary 